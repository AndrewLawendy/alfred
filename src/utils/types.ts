export type Shirt = {
  type: "shirt";
  id: string;
  title: string;
  description: string;
  imageUrl: string;
};

export type Belt = {
  type: "belt";
  id: string;
  title: string;
  description: string;
  imageUrl: string;
};

export type PantsPair = {
  type: "pants";
  id: string;
  title: string;
  description: string;
  imageUrl: string;
};

export type ShoePair = {
  type: "shoes";
  id: string;
  title: string;
  description: string;
  imageUrl: string;
};

export type Jacket = {
  type: "jacket";
  id: string;
  title: string;
  description: string;
  imageUrl: string;
  maxTemperature: number;
};
